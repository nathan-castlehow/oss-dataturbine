# a crontab to keep doing nmi runs
# $LastChangedDate$
# $LastChangedRevision$
# $LastChangedBy$
# $HeadURL$

@daily	(. /nmi/bin/config.sh && cd $HOME/nmi_rbnb && nmi_submit cleosRbnb.submit > $HOME/nmi_rbnb/lastRbnbBuild.log 2>&1)
